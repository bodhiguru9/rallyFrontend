import { StyleSheet } from 'react-native';
import { colors, spacing, borderRadius } from '@theme';

const TAB_WIDTH = 100;

export const styles = StyleSheet.create({
  tab: {
    width: TAB_WIDTH,
    height: 24,
  },
});

export const TAB_WIDTH_PX = TAB_WIDTH;
